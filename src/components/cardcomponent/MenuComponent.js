import React from 'react'

const MenuComponent = ({accessibility, id, action}) => {
  return (
    // <div> <div>{accessibility.altText}</div>
    //  <div>{id}</div>
     <div>{action}</div>
  )
}

export default MenuComponent